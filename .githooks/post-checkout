#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"

if [ -x "$repo_root/scripts/codex/bootstrap.sh" ]; then
  "$repo_root/scripts/codex/bootstrap.sh"
fi

